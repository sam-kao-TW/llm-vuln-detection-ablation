# Runtime dependencies for the notebooks.
#
# Pinned to versions known to work as of May 2026.
# Reviewers may relax these to nearby versions; the API surface used is stable.

openai>=1.30.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
statsmodels>=0.14.0,<1.0.0
tqdm>=4.66.0,<5.0.0

# Jupyter for running the notebooks (use jupyter lab or classic notebook):
jupyter>=1.0.0
